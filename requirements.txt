# Runtime dependencies for perfect-skill-suggester
# Install with: pip install -r requirements.txt
#           or: uv pip install -r requirements.txt
# NOTE: pyproject.toml is the source of truth. Use `uv sync` for development.

# YAML parsing for skill metadata
pyyaml>=6.0

# Anthropic SDK (used by pss_hook.py for model calls)
anthropic>=0.84.0
