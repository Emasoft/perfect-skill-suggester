{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PSS Agent TOML Configuration",
  "description": "Schema for .agent.toml files generated by /pss-setup-agent. Defines the recommended skills, complementary agents, and metadata for a profiled agent. NOTE: A .agent.toml defines a reusable role-plugin profile, NOT a specific AI Maestro agent/persona. The same role-plugin can be assigned to many personas simultaneously. Do NOT add persona names, nicknames, or agent-specific identifiers to the profile.",
  "version": "1.0",
  "type": "object",
  "required": ["agent", "skills"],
  "properties": {
    "agent": {
      "type": "object",
      "description": "Agent identification and source metadata",
      "required": ["name", "path"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Agent name (kebab-case). For Role-Plugins, this MUST match both the plugin name (from plugin.json) and the .agent.toml filename stem. This triple-match rule (plugin name == filename stem == [agent].name) is how AI Maestro identifies Role-Plugins.",
          "pattern": "^[a-z0-9][a-z0-9_-]*$"
        },
        "source": {
          "type": "string",
          "description": "How the agent was specified: 'path' for direct file path, 'plugin:<name>' or 'plugin:<owner>/<name>' for plugin notation",
          "pattern": "^(path|plugin:[a-z0-9][a-z0-9/_-]*)$",
          "examples": ["path", "plugin:my-plugin", "plugin:emasoft/my-plugin"]
        },
        "path": {
          "type": "string",
          "description": "Absolute path to the <agent-name>.md definition file"
        },
        "effort": {
          "type": "string",
          "description": "Model effort level for this agent (CC v2.1.78+). Controls thinking depth. `xhigh` is Opus 4.7's extension beyond `high` (CC v2.1.111+); non-Opus-4.7 models silently fall back to `high`.",
          "enum": ["low", "medium", "high", "xhigh"],
          "examples": ["medium"]
        },
        "maxTurns": {
          "type": "integer",
          "description": "Maximum number of turns the agent can take before stopping (CC v2.1.78+).",
          "minimum": 1
        },
        "disallowedTools": {
          "type": "array",
          "description": "Tools this agent is not allowed to use (CC v2.1.78+). Uses same syntax as settings disallowedTools.",
          "items": { "type": "string" },
          "examples": [["WebSearch", "WebFetch"]]
        }
      },
      "additionalProperties": false
    },
    "description": {
      "type": "object",
      "description": "Human-readable description of the agent role, used for plugin.json and README generation",
      "properties": {
        "text": {
          "type": "string",
          "description": "Description of the role-plugin for README and plugin.json",
          "default": ""
        }
      },
      "additionalProperties": false
    },
    "requirements": {
      "type": "object",
      "description": "Design requirements context used for profiling (empty if none provided)",
      "properties": {
        "files": {
          "type": "array",
          "description": "Basenames of requirement files used for profiling",
          "items": { "type": "string" },
          "default": []
        },
        "project_type": {
          "type": "string",
          "description": "Detected project type from requirements (e.g., 'web-app', 'cli-tool', 'mobile-app', 'library')",
          "default": ""
        },
        "tech_stack": {
          "type": "array",
          "description": "Key technologies detected from requirements",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "skills": {
      "type": "object",
      "description": "Tiered skill recommendations, ordered by relevance within each tier",
      "required": ["primary", "secondary", "specialized"],
      "properties": {
        "primary": {
          "type": "array",
          "description": "Core skills the agent needs for daily work (min 1, max 7). Score >= 60% of max.",
          "items": { "type": "string" },
          "minItems": 1,
          "maxItems": 7,
          "uniqueItems": true
        },
        "secondary": {
          "type": "array",
          "description": "Useful skills for common tasks (max 12). Score 30-59% of max.",
          "items": { "type": "string" },
          "maxItems": 12,
          "uniqueItems": true
        },
        "specialized": {
          "type": "array",
          "description": "Niche skills for specific situations (max 8). Score 15-29% of max.",
          "items": { "type": "string" },
          "maxItems": 8,
          "uniqueItems": true
        },
        "excluded": {
          "type": "object",
          "description": "Skills considered but excluded. Keys are skill names, values are exclusion reasons. Stored as TOML comments in the file (not parsed, for human reference).",
          "additionalProperties": { "type": "string" }
        }
      },
      "additionalProperties": false
    },
    "agents": {
      "type": "object",
      "description": "Agents that complement this agent's workflow (spawned as helpers or used alongside)",
      "properties": {
        "recommended": {
          "type": "array",
          "description": "Agent names that complement this agent",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "commands": {
      "type": "object",
      "description": "Recommended slash commands for this agent",
      "properties": {
        "recommended": {
          "type": "array",
          "description": "Command names that enhance this agent's workflow",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "rules": {
      "type": "object",
      "description": "Rules that should be active when this agent runs",
      "properties": {
        "recommended": {
          "type": "array",
          "description": "Rule names that enforce quality constraints for this agent",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "mcp": {
      "type": "object",
      "description": "MCP servers that enhance this agent's capabilities",
      "properties": {
        "recommended": {
          "type": "array",
          "description": "MCP server names that enhance this agent's capabilities",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "hooks": {
      "type": "object",
      "description": "Hooks relevant to this agent's workflow",
      "properties": {
        "recommended": {
          "type": "array",
          "description": "Hook configurations relevant to this agent's workflow",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "lsp": {
      "type": "object",
      "description": "LSP servers relevant to this agent",
      "properties": {
        "recommended": {
          "type": "array",
          "description": "LSP server names for languages this agent works with",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "output_styles": {
      "type": "object",
      "description": "Output formatting styles for this agent",
      "properties": {
        "recommended": {
          "type": "array",
          "description": "Output style names that customize this agent's output formatting",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "dependencies": {
      "type": "object",
      "description": "External plugins, skills, or tools required for this agent to function. `plugins` is propagated verbatim into plugin.json's top-level `dependencies` array (Claude Code v2.1.110+) so Claude Code resolves and auto-installs them. The remaining arrays (skills/agents/commands/rules/mcp_servers/etc.) are documentation-only — Claude Code does not act on them, but consumers can read them to verify prerequisites.",
      "properties": {
        "plugins": {
          "type": "array",
          "description": "Plugin dependencies emitted into plugin.json's `dependencies` array. Each entry is either a bare plugin name string OR an object with `{name, version, marketplace}`. `version` is an npm semver range (e.g. `~2.1.0`, `^2.0`, `>=1.4`); `marketplace` lets a plugin depend on another marketplace (requires `allowCrossMarketplaceDependenciesOn` in the root marketplace.json). See https://code.claude.com/docs/en/plugin-dependencies for full semantics, resolution rules, and error codes (`dependency-unsatisfied`, `range-conflict`, `dependency-version-unsatisfied`, `no-matching-tag`).",
          "items": {
            "oneOf": [
              {
                "type": "string",
                "description": "Bare plugin name (resolves within the same marketplace, tracks latest version)",
                "pattern": "^[a-z0-9][a-z0-9_-]*$"
              },
              {
                "type": "object",
                "description": "Plugin dependency object with optional version constraint and/or alternate marketplace",
                "required": ["name"],
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Plugin name (required)",
                    "pattern": "^[a-z0-9][a-z0-9_-]*$"
                  },
                  "version": {
                    "type": "string",
                    "description": "npm semver range (e.g. `~2.1.0`, `^2.0`, `>=1.4`, `=2.1.0`). Pre-releases excluded unless explicitly opted in (e.g. `^2.0.0-0`). Resolved against `{plugin-name}--v{version}` git tags on the marketplace repo.",
                    "examples": ["~2.1.0", "^2.0", ">=1.4", "=2.1.0"]
                  },
                  "marketplace": {
                    "type": "string",
                    "description": "Cross-marketplace dependency. The target marketplace must be listed in the root marketplace.json's `allowCrossMarketplaceDependenciesOn` array, otherwise install fails with `cross-marketplace` error.",
                    "examples": ["acme-shared"]
                  }
                },
                "additionalProperties": false
              }
            ]
          },
          "default": [],
          "examples": [
            ["audit-logger"],
            [{ "name": "secrets-vault", "version": "~2.1.0" }],
            ["audit-logger", { "name": "secrets-vault", "version": "~2.1.0" }, { "name": "shared-utils", "marketplace": "acme-shared" }]
          ]
        },
        "skills": {
          "type": "array",
          "description": "Skill names from external plugins that must be available globally",
          "items": { "type": "string" },
          "default": []
        },
        "mcp_servers": {
          "type": "array",
          "description": "MCP server names that must be configured in settings",
          "items": { "type": "string" },
          "default": []
        },
        "tools": {
          "type": "array",
          "description": "CLI tools that must be on PATH (e.g., 'gh', 'docker', 'terraform')",
          "items": { "type": "string" },
          "default": []
        },
        "scripts": {
          "type": "array",
          "description": "CLI scripts that must be on PATH (e.g., 'amp-send.sh', 'aimaestro-agent.sh')",
          "items": { "type": "string" },
          "default": []
        },
        "hooks": {
          "type": "array",
          "description": "Hook configurations that must be installed globally (e.g., hook names from settings.json)",
          "items": { "type": "string" },
          "default": []
        },
        "rules": {
          "type": "array",
          "description": "Rule names that must be available",
          "items": { "type": "string" },
          "default": []
        },
        "agents": {
          "type": "array",
          "description": "Agent names that must be available",
          "items": { "type": "string" },
          "default": []
        },
        "commands": {
          "type": "array",
          "description": "Command names that must be available",
          "items": { "type": "string" },
          "default": []
        },
        "lsp_servers": {
          "type": "array",
          "description": "LSP server names that must be configured",
          "items": { "type": "string" },
          "default": []
        },
        "output_styles": {
          "type": "array",
          "description": "Output style names that must be available",
          "items": { "type": "string" },
          "default": []
        },
        "frameworks": {
          "type": "array",
          "description": "Frameworks that must be installed (e.g., 'django', 'react')",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "metadata": {
      "type": "object",
      "description": "Optional plugin provenance fields propagated into the generated plugin.json by /pss-make-plugin-from-profile. All fields are optional — emitted only when present.",
      "properties": {
        "homepage": {
          "type": "string",
          "description": "Plugin homepage URL (e.g., https://github.com/user/repo)",
          "format": "uri"
        },
        "repository": {
          "type": "string",
          "description": "Source repository URL (e.g., https://github.com/user/repo.git)",
          "format": "uri"
        },
        "license": {
          "type": "string",
          "description": "SPDX license identifier (e.g., MIT, Apache-2.0)"
        },
        "display_name": {
          "type": "string",
          "description": "Human-readable plugin name shown in the /plugin picker and other UI (emitted as plugin.json displayName, CC v2.1.143+). Falls back to name when omitted; may contain spaces and any casing."
        },
        "default_enabled": {
          "type": "boolean",
          "description": "Whether the generated plugin starts enabled when the user has not set a preference (emitted as plugin.json defaultEnabled, CC v2.1.154+). Defaults to true when omitted; set false to ship a plugin that installs disabled."
        }
      },
      "additionalProperties": false
    },
    "userConfig": {
      "type": "object",
      "description": "Optional userConfig section propagated verbatim into the generated plugin.json. Pass-through: PSS does NOT validate the nested structure. Consumers must follow the Claude Code plugins-reference.md userConfig schema (configurable settings exposed to end users when the plugin is installed). Example: { apiKey = { type = 'string', required = true } }",
      "additionalProperties": true
    },
    "monitors": {
      "description": "Optional monitors propagated into the generated plugin.json under `experimental.monitors` (CC v2.1.105+; nesting under `experimental` enforced as of CC v2.1.129). CC types this as an ARRAY of monitor entries OR a relative path string — NOT an inline object. Declare inline as `[[monitors]]` array-of-tables, or point at a file with `monitors = \"./config/monitors.json\"`. Each monitor `command` must NOT reference ${user_config.*} (rejected by CC 2.1.207). Consumers must follow the Claude Code plugins-reference.md monitors schema.",
      "oneOf": [
        {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["name", "command", "description"],
            "properties": {
              "name": { "type": "string" },
              "command": { "type": "string" },
              "description": { "type": "string" },
              "when": { "type": "string", "description": "\"always\" or \"on-skill-invoke:<skill>\"" }
            },
            "additionalProperties": true
          }
        },
        { "type": "string" }
      ]
    },
    "themes": {
      "description": "Optional themes propagated into the generated plugin.json under `experimental.themes` (CC v2.1.118+; nesting under `experimental` enforced as of CC v2.1.129). CC types this as a path STRING or an ARRAY of path strings pointing at theme JSON files/dirs — NOT an inline object (a theme is a JSON file in `themes/`). Set `themes = \"./themes\"` or `themes = [\"./themes/a.json\", \"./themes/b.json\"]`. Consumers must follow the Claude Code plugins-reference.md themes schema.",
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ]
    },
    "channels": {
      "type": "array",
      "description": "Optional channels section propagated verbatim into the generated plugin.json. Channel declarations for message injection (Telegram, Slack, Discord style) that bind to an MCP server provided by the same plugin. Pass-through: PSS does NOT validate the nested structure. Consumers must follow the Claude Code plugins-reference.md channels schema.",
      "items": { "type": "object", "additionalProperties": true }
    },
    "data_dir": {
      "type": "object",
      "description": "Optional directives for runtime dependency installation into the generated plugin's `${CLAUDE_PLUGIN_DATA}` directory via SessionStart hooks. When present, the generator emits a SessionStart hook that lazily installs declared package-manager deps the first time the plugin loads and after any version that bumps the bundled manifest. The directory survives plugin updates (per CC plugins-reference) and is cleaned up by `claude plugin uninstall` unless `--keep-data` is passed.",
      "properties": {
        "npm": {
          "type": "string",
          "description": "Relative path (from plugin root) to a `package.json` whose dependencies should be installed into ${CLAUDE_PLUGIN_DATA}/node_modules on first session start. Generated hook re-runs `npm install` only when the bundled file differs from the copy in ${CLAUDE_PLUGIN_DATA}.",
          "examples": ["./package.json"]
        },
        "pip": {
          "type": "string",
          "description": "Relative path (from plugin root) to a Python requirements file (`requirements.txt` or `pyproject.toml`) whose dependencies should be installed into ${CLAUDE_PLUGIN_DATA}/.venv via `uv pip install` on first session start.",
          "examples": ["./requirements.txt", "./pyproject.toml"]
        },
        "rust_cargo": {
          "type": "string",
          "description": "Relative path (from plugin root) to a `Cargo.toml` whose binary targets should be built into ${CLAUDE_PLUGIN_DATA}/bin on first session start.",
          "examples": ["./Cargo.toml"]
        },
        "downloads": {
          "type": "array",
          "description": "List of URL+sha256+dest triples to download into ${CLAUDE_PLUGIN_DATA} on first session start (e.g. model files, large fixtures). Each download is verified against its sha256 before use.",
          "items": {
            "type": "object",
            "required": ["url", "sha256", "dest"],
            "properties": {
              "url": { "type": "string", "format": "uri" },
              "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
              "dest": { "type": "string", "description": "Path relative to ${CLAUDE_PLUGIN_DATA}" }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false,

  "examples": [
    {
      "agent": {
        "name": "security-auditor",
        "source": "path",
        "path": "/path/to/agents/security-auditor.md"
      },
      "description": {
        "text": "Security auditor agent that performs vulnerability scanning, code review, and compliance checks"
      },
      "requirements": {
        "files": ["prd.md", "tech-spec.md"],
        "project_type": "web-app",
        "tech_stack": ["python", "django", "postgresql"]
      },
      "skills": {
        "primary": ["security", "aegis", "code-review"],
        "secondary": ["exhaustive-testing", "dead-code", "debug-hooks"],
        "specialized": ["tdd", "profiler"],
        "excluded": {}
      },
      "agents": {
        "recommended": ["sleuth", "e2e-tester"]
      },
      "commands": { "recommended": ["pss-reindex-skills"] },
      "rules": { "recommended": ["claim-verification"] },
      "mcp": { "recommended": ["chrome-devtools"] },
      "hooks": { "recommended": [] },
      "lsp": { "recommended": ["pyright-lsp"] },
      "output_styles": { "recommended": [] },
      "dependencies": { "plugins": [], "skills": [], "mcp_servers": ["chrome-devtools"], "tools": ["gh"], "scripts": [], "hooks": [], "rules": [], "agents": [], "commands": [], "lsp_servers": [], "output_styles": [], "frameworks": [] }
    },
    {
      "agent": {
        "name": "frontend-developer",
        "source": "plugin:multi-platform-apps",
        "path": "~/.claude/plugins/cache/abc/multi-platform-apps/1.0/agents/frontend-developer.md"
      },
      "description": {
        "text": "Frontend developer agent specialized in React/Next.js web applications with TypeScript and Tailwind CSS"
      },
      "requirements": {
        "files": ["prd.md", "design-system.md"],
        "project_type": "web-app",
        "tech_stack": ["typescript", "react", "nextjs", "tailwind", "postgresql"]
      },
      "skills": {
        "primary": ["building-with-bun", "css-to-svg-conversion", "development-standards"],
        "secondary": ["exhaustive-testing", "handle-deprecation-warnings", "dependency-management", "git-workflow"],
        "specialized": ["accessibility-compliance", "responsive-design"],
        "excluded": {
          "vue-frontend": "Excluded: conflicts with React (requirements specify React/Next.js)",
          "angular-dev": "Excluded: conflicts with React (requirements specify React/Next.js)"
        }
      },
      "agents": {
        "recommended": ["ui-ux-designer", "e2e-tester"]
      },
      "commands": { "recommended": ["commit", "describe-pr"] },
      "rules": { "recommended": ["claim-verification", "observe-before-editing"] },
      "mcp": { "recommended": ["chrome-devtools"] },
      "hooks": { "recommended": [] },
      "lsp": { "recommended": ["typescript-lsp"] },
      "output_styles": { "recommended": [] },
      "dependencies": {
        "plugins": [
          "audit-logger",
          { "name": "secrets-vault", "version": "~2.1.0" },
          { "name": "shared-utils", "marketplace": "acme-shared" }
        ],
        "skills": [],
        "mcp_servers": ["chrome-devtools"],
        "tools": ["bun", "gh"],
        "scripts": [],
        "hooks": [],
        "rules": [],
        "agents": [],
        "commands": [],
        "lsp_servers": [],
        "output_styles": [],
        "frameworks": []
      },
      "data_dir": {
        "npm": "./package.json"
      }
    }
  ]
}
